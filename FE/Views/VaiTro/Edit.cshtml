@model BE.models.VaiTro
@{
    ViewData["Title"] = "Chỉnh sửa vai trò";
    Layout = "_AdminLayout";
}

<div class="card shadow border-0">
    <div class="card-header bg-warning text-white">
        <h4 class="mb-0"><i class="fas fa-edit me-2"></i>Chỉnh sửa vai trò</h4>
    </div>
    <div class="card-body">
        <form asp-action="Edit" method="post">
            <input type="hidden" asp-for="ID_Vai_Tro" />
            <div class="mb-3">
                <label asp-for="Ten_Vai_Tro" class="form-label"></label>
                <input asp-for="Ten_Vai_Tro" class="form-control" placeholder="Nhập tên vai trò..." />
                <span asp-validation-for="Ten_Vai_Tro" class="text-danger"></span>
            </div>
            <div class="d-flex justify-content-end">
                <a asp-action="Index" class="btn btn-secondary me-2"><i class="fas fa-arrow-left"></i> Quay lại</a>
                <button type="submit" class="btn btn-warning text-white"><i class="fas fa-save"></i> Cập nhật</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
